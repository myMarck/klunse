plugins {
    id('java-platform')
}

group = 'org.klunse.platform'

javaPlatform.allowDependencies()

dependencies {
    api(platform('org.springframework.boot:spring-boot-dependencies:3.4.1'))

    constraints {
        api('org.apache.juneau:juneau-marshall:9.0.1')
    }
}
