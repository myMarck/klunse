plugins {
    id('groovy-gradle-plugin')
}

dependencies {
    implementation(platform('org.klunse.platform:plugins-platform'))
    implementation(project(':commons'))
    implementation('org.springframework.boot:org.springframework.boot.gradle.plugin')
}
